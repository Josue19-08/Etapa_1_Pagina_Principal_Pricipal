import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ease_order_pagina_principal',
  webDir: 'www'
};

export default config;
